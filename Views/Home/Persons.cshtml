@model List<PersonModel>;

<table>
    <tr>
        <td>Nome</td>
        <td>Cognome</td>
    </tr>
    @foreach (PersonModel p in Model)
    {
        <tr>
            <td>@p.Firstname</td>
            <td>@p.Lastname</td>
        </tr>
    }
</table>